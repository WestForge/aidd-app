import type { DeliveryBundle } from '../domain/types';
import { Button } from './ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from './ui/card';
import { Textarea } from './ui/textarea';
import { Badge } from './ui/badge';
import { statusPillClass } from '../lib/statusTheme';
export function Verification({ bundle, onChange }: { bundle: DeliveryBundle; onChange: (bundle: DeliveryBundle) => void }) { return <div className="flex h-full flex-col overflow-hidden"><header className="flex h-16 shrink-0 items-center justify-between border-b px-6"><div><h1 className="text-xl font-semibold">Verification</h1><p className="text-sm text-muted-foreground">Record evidence for {bundle.id}.</p></div><Badge variant="outline" className={statusPillClass(bundle.status)}>{bundle.status.replace(/-/g, ' ')}</Badge></header><main className="min-h-0 flex-1 overflow-auto p-6"><Card className="max-w-4xl"><CardHeader><CardTitle>{bundle.title}</CardTitle><CardDescription>Capture verification notes and accept the package when complete.</CardDescription></CardHeader><CardContent className="space-y-4"><Textarea className="min-h-[240px]" value={bundle.verificationNotes} onChange={(e) => onChange({ ...bundle, verificationNotes: e.target.value })} /><div className="flex gap-2"><Button variant="outline" onClick={() => onChange({ ...bundle, status: 'needs-verification' })}>Needs verification</Button><Button onClick={() => onChange({ ...bundle, status: 'accepted' })}>Accept package</Button></div></CardContent></Card></main></div>; }
